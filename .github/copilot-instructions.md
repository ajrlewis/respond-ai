# Copilot Instructions

Use `AGENTS.md` at the repository root as the primary instruction entrypoint.

Before producing or editing code, read `AGENTS.md` and follow the referenced `.agents/*` files in the order specified there.
